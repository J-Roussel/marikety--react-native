import { StyleSheet } from "react-native"

export const logoStyles = StyleSheet.create({
    logoContainer: {
        alignItems: 'center',
        marginBottom:0
      },
    logo :{
        width: 240,
        height: 150,
        borderRadius:80,
        resizeMode: 'contain',
    },
})